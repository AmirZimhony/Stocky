<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stocks</title>
    <link rel="icon" type="image/png" href="reading.jpg">
</head>

<body>
    <h1>All stocks</h1>

    <div>
        <a href="/stocks/new">add stock</a>
    </div>

    <ul id = 'stockList'>
        <!-- <button id="btn"> For Freshest IPOs</button> -->
       <% const trendest = stocks.filter(stock => stock.numOfRisingDays > 4 && stock.IPOYear>2018)   %> <!--maybe useful in future-->
        <% for (let stock of stocks) { %>
            <li><a href="/stocks/<%= stock.id  %> ">
                    <%= stock.Symbol %> , <%= stock.Name %> 
                </a></li>
            <% } %>
    </ul>
</body>

</html>